import SEARCHTYPE from "./action";

const initialState = {
  searchvalue: "",
  searchtype: "anime",
  isLoading: true,
  db: []
};

const searchReducer = (state = initialState, action) => {
  switch (action.type) {
    case action.SEARCHTYPE:
  }
};
